# GlobeEngine

Modular Three.js globe engine.

## Install
npm install

## Dev
npm run dev

## Lifecycle
const engine = new GlobeEngine(container)
engine.start()
engine.stop()
engine.destroy()

## Architecture
- GlobeEngine (core)
- Controllers
- Scene objects
